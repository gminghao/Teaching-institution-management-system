# 开发环境配置
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.institution.coursemanager: debug

jwt:
  secret: DevOnlySecretKeyForLocalDevelopmentMustBe256Bits!!
  expiration: 7200000