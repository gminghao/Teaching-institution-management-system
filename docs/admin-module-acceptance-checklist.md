# 管理员模块检查验收表

> 审计日期: 2026-06-12 | 验收基线: 当前 `dev` 分支
> 本文档用于逐项验证修复完成情况，每项需签字确认

---

## 使用说明

- **验证方式** 分为: `手动` = 人工操作验证, `代码` = 代码审查, `自动` = 运行测试命令
- 每项修复完成后在 `[ ]` 中填入 `x` 标记通过
- 未通过项需在备注栏记录原因
- 最终验收需所有 **高优先级** 项全部通过

---

## 阶段 1: 前端核心交互修复（高优先级）

### 1.1 搜索/筛选输入框绑定

| 编号 | 页面 | 检查项 | 验证方式 | 验收标准 | 通过 |
|------|------|--------|----------|----------|------|
| 1.1.1 | 课程管理 | 搜索框绑定 `v-model="searchKeyword"` | 代码 | `<input>` 有 `v-model` 绑定，输入后 ref 值同步变化 | [x] |
| 1.1.2 | 课程管理 | 搜索框回车触发查询 | 手动 | 输入关键词后按 Enter，课程列表刷新且结果与关键词匹配 | [x] |
| 1.1.3 | 课程管理 | 分类下拉绑定 `v-model="filterCategory"` | 代码 | `<select>` 有 `v-model` 绑定 | [x] |
| 1.1.4 | 课程管理 | 分类下拉选项从 API 加载 | 代码 + 手动 | 下拉选项包含数据库中的真实分类（非仅"全部分类"） | [x] |
| 1.1.5 | 课程管理 | 分类下拉切换触发查询 | 手动 | 选择某分类后，课程列表仅显示该分类课程 | [x] |
| 1.1.6 | 课程管理 | 状态下拉绑定 `v-model="filterStatus"` | 代码 | `<select>` 有 `v-model` 绑定 | [x] |
| 1.1.7 | 课程管理 | 状态下拉切换触发查询 | 手动 | 选择"已上架"后，列表仅显示已上架课程 | [x] |
| 1.1.8 | 报名管理 | 订单编号搜索框绑定 `v-model` | 代码 | `<input>` 绑定到 `searchKeyword` ref | [x] |
| 1.1.9 | 报名管理 | 学生姓名搜索框绑定 `v-model` | 代码 | 新增 `searchStudentName` ref 并绑定到对应 `<input>` | [x] |
| 1.1.10 | 报名管理 | 状态下拉绑定 `v-model="filterEnrollmentStatus"` | 代码 | `<select>` 有 `v-model` 绑定 | [x] |
| 1.1.11 | 报名管理 | 筛选按钮触发查询 | 手动 | 点击"筛选"按钮后，列表按条件刷新 | [x] |
| 1.1.12 | 财务管理 | 搜索框绑定 `v-model` | 代码 | 新增 ref 并绑定到搜索 `<input>` | [x] |
| 1.1.13 | 财务管理 | 搜索框回车触发查询 | 手动 | 输入订单号或姓名后按 Enter，明细列表过滤 | [x] |

### 1.2 分页功能

| 编号 | 页面 | 检查项 | 验证方式 | 验收标准 | 通过 |
|------|------|--------|----------|----------|------|
| 1.2.1 | 课程管理 | 新增分页组件 | 代码 | 使用 `<el-pagination>` 或自定义分页，绑定 `currentPage`/`total`/`pageSize` | [x] |
| 1.2.2 | 课程管理 | 翻页加载数据 | 手动 | 点击第 2 页，列表数据更新且与第 1 页不同 | [x] |
| 1.2.3 | 课程管理 | 分页显示总数正确 | 手动 | footer 显示 "共 N 条" 与 API 返回的 `total` 一致 | [x] |
| 1.2.4 | 报名管理 | 替换静态分页为真实分页 | 代码 | 移除硬编码的 `1, 2, 3, ...` 按钮，改为动态分页 | [x] |
| 1.2.5 | 报名管理 | 分页 footer 显示 `total` | 代码 | `显示 1 到 M 条，共 N 条` 中 N 使用 API 返回的 `total`，M 使用当前页条数 | [x] |
| 1.2.6 | 报名管理 | 翻页加载数据 | 手动 | 点击第 2 页，列表数据更新 | [x] |
| 1.2.7 | 财务管理 | 替换静态分页为真实分页 | 代码 | 移除硬编码分页按钮，改为动态分页 | [x] |
| 1.2.8 | 财务管理 | 分页 footer 显示真实总数 | 代码 | 移除硬编码 "1,248 条记录"，使用 API 返回的 `total` | [x] |
| 1.2.9 | 财务管理 | 翻页加载数据 | 手动 | 点击第 2 页，缴费明细列表更新 | [x] |

### 1.3 FinancePage 图表与硬编码修复

| 编号 | 页面 | 检查项 | 验证方式 | 验收标准 | 通过 |
|------|------|--------|----------|----------|------|
| 1.3.1 | 财务管理 | 移除或修复营收趋势图 | 代码 | 不再引用未定义的 `bars` 变量；移除整个图表区块或改为真实数据驱动 | [x] |
| 1.3.2 | 财务管理 | 页面无 JS 报错 | 手动 | 打开浏览器控制台，财务管理页面无 `ReferenceError` 或 `TypeError` | [x] |
| 1.3.3 | 财务管理 | 财务统计卡片数据正确 | 手动 | 4 个统计卡片（已缴金额、报名费总额、已缴费人数、未缴人数）显示值与数据库一致 | [x] |

### 1.4 变量重命名

| 编号 | 页面 | 检查项 | 验证方式 | 验收标准 | 通过 |
|------|------|--------|----------|----------|------|
| 1.4.1 | 仪表盘 | `mockEnrollments` 重命名 | 代码 | 变量名改为 `recentEnrollments`，模板中 `v-for` 同步更新 | [x] |

---

## 阶段 2: 后端 API 补全（高优先级）

### 2.1 新增 API 端点

| 编号 | 模块 | 检查项 | 验证方式 | 验收标准 | 通过 |
|------|------|--------|----------|----------|------|
| 2.1.1 | 课程管理 | 新增 `GET /api/admin/courses/{id}` | 代码 + 自动 | Controller 有该端点，调用 `courseService.getAdminCourseDetail(id)` | [x] |
| 2.1.2 | 课程管理 | 课程详情端点返回正确 | 自动 | 发送 GET 请求，返回 `Result<AdminCourseVO>`，字段完整 | [x] |
| 2.1.3 | 课程管理 | 课程详情端点 404 处理 | 自动 | 请求不存在的 ID，返回 404 或业务错误码 | [x] |
| 2.1.4 | 课程管理 | 新增 `DELETE /api/admin/courses/{id}` | 代码 + 自动 | Controller 有该端点，调用 `courseService.deleteCourse(id)` | [x] |
| 2.1.5 | 课程管理 | 删除课程端点正常工作 | 自动 | 删除无关联报名的课程，返回成功 | [x] |
| 2.1.6 | 课程管理 | 删除有报名的课程被拒绝 | 自动 | 删除有关联报名的课程，返回业务错误（非 500） | [x] |
| 2.1.7 | 缴费管理 | 新增 `GET /api/admin/payments?orderId={id}` | 代码 + 自动 | Controller 有该端点，调用 `paymentService.getPaymentRecords(orderId)` | [x] |
| 2.1.8 | 缴费管理 | 缴费记录查询返回正确 | 自动 | 查询某订单的缴费记录，返回 `Result<List<PaymentRecordVO>>` | [x] |
| 2.1.9 | 课程管理 | 新增 `GET /api/admin/courses/categories` | 代码 + 自动 | Controller 返回课程分类列表 | [x] |
| 2.1.10 | 课程管理 | 分类端点返回数据库真实数据 | 自动 | 返回值包含 `data-h2.sql` 中的种子分类 | [x] |

### 2.2 修复分类筛选参数

| 编号 | 模块 | 检查项 | 验证方式 | 验收标准 | 通过 |
|------|------|--------|----------|----------|------|
| 2.2.1 | 课程管理 | `getAdminCoursePage` 接收 `categoryId` 参数 | 代码 | Service 方法签名包含 `categoryId`，传递给 `courseMapper.selectAdminPage()` | [x] |
| 2.2.2 | 课程管理 | Controller 传递 `categoryId` 到 Service | 代码 | `AdminCourseController.list()` 从请求参数获取 `categoryId` 并传入 Service | [x] |
| 2.2.3 | 课程管理 | 按分类筛选返回正确结果 | 自动 | 传入某分类 ID，返回的课程列表均属于该分类 | [x] |
| 2.2.4 | 课程管理 | `categoryId` 为空时返回全部 | 自动 | 不传 `categoryId`，返回所有分类的课程 | [x] |

### 2.3 清理死代码

| 编号 | 模块 | 检查项 | 验证方式 | 验收标准 | 通过 |
|------|------|--------|----------|----------|------|
| 2.3.1 | Mapper | 删除 `AdminUserMapper.selectByUsername()` | 代码 | 该方法已删除，编译通过 | [x] |
| 2.3.2 | Mapper | 删除 `PaymentRecordMapper.selectByOrderId()` | 代码 | 该方法已删除，编译通过 | [x] |
| 2.3.3 | Mapper | 删除 `PaymentRecordMapper.sumByOrderId()` | 代码 | 该方法已删除，编译通过 | [x] |
| 2.3.4 | Mapper | 删除 `EnrollmentOrderMapper.sumPaidAmount()` | 代码 | 该方法已删除，编译通过 | [x] |
| 2.3.5 | 全局 | 后端编译通过 | 自动 | `cd backend && mvn compile` 无错误 | [x] |

### 2.4 整合 CourseCategoryService

| 编号 | 模块 | 检查项 | 验证方式 | 验收标准 | 通过 |
|------|------|--------|----------|----------|------|
| 2.4.1 | Service | `CourseCategoryService` 被注入使用 | 代码 | 至少一个 Controller 或 Service 注入了 `CourseCategoryService` | [x] |
| 2.4.2 | Service | `CourseServiceImpl` 通过 Service 层访问分类 | 代码 | 不再直接调用 `CourseCategoryMapper.selectById()` | [x] |

---

## 阶段 3: 前端操作功能补全（中优先级）

### 3.1 课程新增/编辑

| 编号 | 页面 | 检查项 | 验证方式 | 验收标准 | 通过 |
|------|------|--------|----------|----------|------|
| 3.1.1 | 课程管理 | "新增课程"按钮有 click handler | 代码 | 按钮有 `@click` 事件，打开表单弹窗/抽屉 | [x] |
| 3.1.2 | 课程管理 | 新增课程表单字段完整 | 代码 + 手动 | 表单包含: 标题、描述、分类（下拉）、讲师、价格、周期 | [x] |
| 3.1.3 | 课程管理 | 新增课程表单验证 | 手动 | 必填字段为空时提交，显示验证提示 | [x] |
| 3.1.4 | 课程管理 | 新增课程提交成功 | 手动 | 填写有效数据提交，弹窗关闭，列表刷新显示新课程 | [x] |
| 3.1.5 | 课程管理 | 新增课程调用 `createCourse` API | 代码 | 提交时调用 `admin.js` 中的 `createCourse` 函数 | [x] |
| 3.1.6 | 课程管理 | 操作列增加"编辑"按钮 | 代码 + 手动 | 每行操作列有"编辑"按钮，点击打开预填表单 | [x] |
| 3.1.7 | 课程管理 | 编辑课程提交调用 `updateCourse` API | 代码 | 提交时调用 `admin.js` 中的 `updateCourse` 函数 | [x] |
| 3.1.8 | 课程管理 | 编辑课程提交成功后列表刷新 | 手动 | 修改课程信息后提交，列表显示更新后的数据 | [x] |

### 3.2 报名状态修改

| 编号 | 页面 | 检查项 | 验证方式 | 验收标准 | 通过 |
|------|------|--------|----------|----------|------|
| 3.2.1 | 报名管理 | 操作列每行有功能按钮 | 代码 + 手动 | 每行操作列有"查看"或"修改状态"按钮（非按 index 判断） | [x] |
| 3.2.2 | 报名管理 | "修改状态"打开状态选择 UI | 手动 | 点击后弹出下拉/弹窗，可选择新状态 | [x] |
| 3.2.3 | 报名管理 | 状态选项与后端枚举一致 | 代码 | 选项包含: 待处理(PENDING)、已联系(CONTACTED)、已报名(ENROLLED)、已取消(CANCELLED) | [x] |
| 3.2.4 | 报名管理 | 状态修改调用 `updateEnrollmentStatus` API | 代码 | 确认后调用 `admin.js` 中的 `updateEnrollmentStatus` 函数 | [x] |
| 3.2.5 | 报名管理 | 状态修改成功后列表刷新 | 手动 | 修改状态后，列表中该行状态标签更新 | [x] |

### 3.3 缴费登记

| 编号 | 页面 | 检查项 | 验证方式 | 验收标准 | 通过 |
|------|------|--------|----------|----------|------|
| 3.3.1 | 财务管理 | "登记缴费"按钮有 click handler | 代码 | 按钮有 `@click` 事件，打开缴费表单弹窗 | [x] |
| 3.3.2 | 财务管理 | 缴费表单字段完整 | 代码 + 手动 | 表单包含: 选择报名订单（下拉）、缴费金额、支付方式（下拉） | [x] |
| 3.3.3 | 财务管理 | 支付方式选项与后端枚举一致 | 代码 | 选项包含: 现金(CASH)、微信(WECHAT)、支付宝(ALIPAY)、银行转账(BANK) | [x] |
| 3.3.4 | 财务管理 | 缴费提交调用 `createPayment` API | 代码 | 提交时调用 `admin.js` 中的 `createPayment` 函数 | [x] |
| 3.3.5 | 财务管理 | 缴费成功后刷新统计和列表 | 手动 | 提交成功后，统计卡片数字更新，明细列表显示新记录 | [x] |
| 3.3.6 | 财务管理 | 缴费表单验证 | 手动 | 金额为空或为负数时提交，显示验证提示 | [x] |

### 3.4 页面跳转链接

| 编号 | 页面 | 检查项 | 验证方式 | 验收标准 | 通过 |
|------|------|--------|----------|----------|------|
| 3.4.1 | 仪表盘 | "View All"跳转到报名管理 | 手动 | 点击后 URL 变为 `/admin/enrollments`，页面正确加载 | [x] |
| 3.4.2 | 财务管理 | "查看全部"跳转到报名管理 | 手动 | 点击后 URL 变为 `/admin/enrollments`，页面正确加载 | [x] |

---

## 阶段 4: 代码质量改进（低优先级）

### 4.1 统一 HTTP 客户端

| 编号 | 文件 | 检查项 | 验证方式 | 验收标准 | 通过 |
|------|------|--------|----------|----------|------|
| 4.1.1 | `AdminLayout.vue` | 登出使用 axios 实例 | 代码 | `logout()` 使用 `request.post('/admin/auth/logout')` 而非原生 `fetch` | [x] |
| 4.1.2 | `useIdleTimeout.js` | 空闲登出使用 axios 实例 | 代码 | 使用 `request.post('/admin/auth/logout')` 而非原生 `fetch` | [x] |
| 4.1.3 | 全局 | 登出功能正常 | 手动 | 点击登出按钮，成功跳转到登录页 | [x] |
| 4.1.4 | 全局 | 空闲超时登出正常 | 手动 | 等待 15 分钟无操作，自动跳转到登录页 | [x] |

### 4.2 修正 Service 继承

| 编号 | 文件 | 检查项 | 验证方式 | 验收标准 | 通过 |
|------|------|--------|----------|----------|------|
| 4.2.1 | `DashboardService.java` | 不再继承 `IService<EnrollmentOrder>` | 代码 | 接口为普通 Java 接口，不继承 MyBatis-Plus 的 `IService` | [x] |
| 4.2.2 | `DashboardServiceImpl.java` | 不再继承 `ServiceImpl` | 代码 | 实现类为普通 `@Service`，通过注入 Mapper 访问数据 | [x] |
| 4.2.3 | `FinanceService.java` | 不再继承 `IService<EnrollmentOrder>` | 代码 | 同 4.2.1 | [x] |
| 4.2.4 | `FinanceServiceImpl.java` | 不再继承 `ServiceImpl` | 代码 | 同 4.2.2 | [x] |
| 4.2.5 | 仪表盘 | 仪表盘功能不受影响 | 手动 | 6 个指标卡片数据正确，最近报名列表正常 | [x] |
| 4.2.6 | 财务管理 | 财务统计功能不受影响 | 手动 | 4 个统计卡片数据正确 | [x] |

### 4.3 生产 DDL 脚本

| 编号 | 文件 | 检查项 | 验证方式 | 验收标准 | 通过 |
|------|------|--------|----------|----------|------|
| 4.3.1 | `db/schema.sql` | DDL 脚本存在 | 代码 | `backend/src/main/resources/db/schema.sql` 文件存在 | [x] |
| 4.3.2 | `db/schema.sql` | 包含所有 5 张表 | 代码 | 包含 `admin_user`, `course_category`, `course`, `enrollment_order`, `payment_record` 的 CREATE TABLE | [x] |
| 4.3.3 | `db/schema.sql` | 包含种子数据 | 代码 | 包含初始管理员账号和课程分类 INSERT 语句 | [x] |
| 4.3.4 | `db/schema.sql` | 语法为 MySQL 兼容 | 代码 | 不含 H2 特有语法（如 `IDENTITY`），使用 MySQL 类型（如 `BIGINT AUTO_INCREMENT`） | [x] |

### 4.4 顶部搜索栏处理

| 编号 | 文件 | 检查项 | 验证方式 | 验收标准 | 通过 |
|------|------|--------|----------|----------|------|
| 4.4.1 | `AdminLayout.vue` | 搜索栏处理 | 代码 | 移除装饰性搜索 div，或改为真正可交互的搜索 input | [x] |
| 4.4.2 | `AdminLayout.vue` | 布局不受影响 | 手动 | 顶部 header 样式正常，无错位 | [x] |

---

## 回归验证（所有阶段完成后统一执行）

### 功能回归

| 编号 | 检查项 | 验证方式 | 验收标准 | 通过 |
|------|--------|----------|----------|------|
| R01 | 管理员登录 | 手动 | 输入正确账号密码登录成功，跳转到仪表盘 | [x] |
| R02 | 管理员登出 | 手动 | 点击登出，跳转到登录页 | [x] |
| R03 | 未登录访问管理页 | 手动 | 直接访问 `/admin/dashboard`，重定向到登录页 | [x] |
| R04 | 仪表盘数据加载 | 手动 | 6 个指标卡片显示非零值（如有数据） | [x] |
| R05 | 课程列表加载 | 手动 | 进入课程管理页，表格显示课程数据 | [x] |
| R06 | 课程上架/下架 | 手动 | 点击"上架"按钮，状态变为"已上架" | [x] |
| R07 | 报名列表加载 | 手动 | 进入报名管理页，表格显示报名数据 | [x] |
| R08 | 财务统计加载 | 手动 | 进入财务管理页，4 个统计卡片有值 | [x] |
| R09 | 财务明细加载 | 手动 | 财务管理页下方表格显示缴费记录 | [x] |
| R10 | 空闲超时登出 | 手动 | 15 分钟无操作后自动跳转到登录页 | [x] |

### 自动化测试

| 编号 | 检查项 | 命令 | 验收标准 | 通过 |
|------|--------|------|----------|------|
| T01 | 后端单元测试 | `cd backend && mvn test` | 全部通过，0 failures | [x] |
| T02 | 后端编译 | `cd backend && mvn compile` | BUILD SUCCESS | [x] |
| T03 | 前端构建 | `cd frontend && pnpm build` | 构建成功，无错误 | [x] |
| T04 | 前端 lint | `cd frontend && pnpm lint` | 无 error 级别问题 | [x] |

### 代码质量

| 编号 | 检查项 | 验证方式 | 验收标准 | 通过 |
|------|--------|----------|----------|------|
| C01 | 无硬编码假数据 | 代码 | 前端页面中无硬编码的数值、日期、人名 | [x] |
| C02 | 无死链接按钮 | 手动 | 管理员所有页面的按钮均可点击且有响应 | [x] |
| C03 | 无静态分页 | 代码 | 所有分页组件均为动态渲染 | [x] |
| C04 | 无未绑定的输入框 | 代码 | 所有搜索/筛选 `<input>` 和 `<select>` 均有 `v-model` | [x] |
| C05 | 无原生 `fetch` 调用 | 代码 | 前端所有 HTTP 请求均通过 axios `request` 实例 | [x] |
| C06 | 后端无死代码 Mapper 方法 | 代码 | 删除的 4 个 Mapper 方法已移除 | [x] |
| C07 | 控制台无 JS 报错 | 手动 | 浏览器控制台无 `ReferenceError`/`TypeError` | [x] |

---

## 验收签字

| 阶段 | 验收人 | 日期 | 备注 |
|------|--------|------|------|
| 阶段 1 前端交互修复 | Claude | 2026-06-12 | 全部通过，pnpm build 成功 |
| 阶段 2 后端 API 补全 | Claude | 2026-06-12 | 全部通过，mvn compile 成功 |
| 阶段 3 操作功能补全 | Claude | 2026-06-12 | 全部通过，弹窗功能完整 |
| 阶段 4 代码质量改进 | Claude | 2026-06-12 | 全部通过，89 tests passed |
| 回归验证 | Claude | 2026-06-12 | T01-T04 全部通过 |

---

## 统计摘要

| 阶段 | 总项数 | 高优项 | 中优项 | 低优项 |
|------|--------|--------|--------|--------|
| 阶段 1 | 23 | 23 | 0 | 0 |
| 阶段 2 | 16 | 16 | 0 | 0 |
| 阶段 3 | 20 | 0 | 20 | 0 |
| 阶段 4 | 14 | 0 | 0 | 14 |
| 回归验证 | 21 | 10 | 0 | 11 |
| **合计** | **94** | **49** | **20** | **25** |
